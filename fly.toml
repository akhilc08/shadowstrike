app = "shadowstrike-relay"
primary_region = "ewr"

[build]
dockerfile = "Dockerfile.relay"

[[services]]
internal_port = 9001
protocol = "tcp"

  [[services.ports]]
  port = 80
  handlers = ["http"]

  [[services.ports]]
  port = 443
  handlers = ["tls", "http"]
